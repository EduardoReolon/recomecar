<?php

define('__ENV__', 'dev');
define('__JWT_SECRET__', 'secret');

if ( isset($_SERVER['SERVER_PORT']) && $_SERVER['SERVER_PORT'] != '' )
	define( '__PORT__', ':' . $_SERVER['SERVER_PORT'] );
else
	define( '__PORT__', '' );